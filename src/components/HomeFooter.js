import React from "react";
import { View, Text, StyleSheet, Image } from "react-native";

export default function HomeFooter({ navigation }) {
  
  return <View style={styles.container}>{donne}</View>;
}
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
